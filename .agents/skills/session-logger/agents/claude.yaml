interface:
  display_name: "Session Logger"
  short_description: "Project session logging and automatic safety capture"
  default_prompt: "Use $session-logger to init or activate a project, run a manual framework audit when needed, recover memory, manually checkpoint/end a session, and preserve accepted-plan state with append-only automatic safety entries."
