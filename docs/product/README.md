# Product Documentation

This directory is reserved for shared repo-level product documentation if the workspace later needs it.

Active project product planning should live inside the relevant project, for example:

```text
projects/<project-name>/docs/product/
```

Product docs are not live workflow memory. Session memory belongs under the activated project's `docs/workflow/`.
